mutation AddVideo(
  $title: String!
  $description: String!
  $file: String!
  $thumbnail: String!
) {
  addVideo(
    title: $title
    description: $description
    file: $file
    thumbnail: $thumbnail
  ) {
    id
    title
    description
    thumbnail
    file
  }
}
